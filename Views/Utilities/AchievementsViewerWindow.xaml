<Window x:Class="Moodex.Views.Utilities.AchievementsViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Achievements" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Icon="/Moodex;component/Assets/icon/icon.ico">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="{Binding CurrentName}" 
                   FontSize="16" FontWeight="Bold"
                   Margin="10" TextAlignment="Center"/>

        <Button Grid.Row="1" Grid.Column="0" Content="⟨" FontSize="18" Margin="8"
                Command="{Binding PrevCommand}"/>
        <Border Grid.Row="1" Grid.Column="1" Margin="8">
            <Image Source="{Binding CurrentImagePath}" Stretch="Uniform" />
        </Border>
        <Button Grid.Row="1" Grid.Column="2" Content="⟩" FontSize="18" Margin="8"
                Command="{Binding NextCommand}"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="8">
            <TextBlock Text="{Binding PositionText}"/>
        </StackPanel>
    </Grid>
</Window>


