<Window x:Class="Moodex.Views.Utilities.AddAchievementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Achievement" Height="140" Width="480"
        WindowStartupLocation="CenterOwner"
        Icon="/Moodex;component/Assets/icon/icon.ico">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="70"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5,5" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Image:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5,5" Text="{Binding ImagePath}" IsReadOnly="True"/>
        <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Margin="5,5" Command="{Binding BrowseCommand}"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Margin="5" Width="90" IsCancel="True"/>
            <Button Content="Add" Margin="5" Width="90" IsDefault="True" Command="{Binding AddCommand}"/>
        </StackPanel>
    </Grid>
    </Window>


