<UserControl
  x:Class="Moodex.Views.Settings.StorageSettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:vm="clr-namespace:Moodex.ViewModels.Settings">

    <StackPanel Margin="10" VerticalAlignment="Top">
        <TextBlock Text="Storage" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- Primary library selection removed -->

        <!-- Active (fast) library -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Library Path:" Width="120" VerticalAlignment="Center"/>
            <Grid Width="300">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding ActiveLibraryPath, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{Binding BrowseLibraryPathCommand}"
                        Margin="8,0,0,0"
                        Width="80"/>
            </Grid>
        </StackPanel>

        <!-- Archive (cold) library -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Archive Path:" Width="120" VerticalAlignment="Center"/>
            <Grid Width="300">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding ArchiveLibraryPath, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{Binding BrowseArchivePathCommand}"
                        Margin="8,0,0,0"
                        Width="80"/>
            </Grid>
            <CheckBox Content="Archive as ZIP"
                      IsChecked="{Binding CompressOnArchive}"
                      VerticalAlignment="Center"
                      Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>

