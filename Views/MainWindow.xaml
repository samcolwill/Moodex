<Window x:Class="Moodex.Views.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:Moodex.Converters"
        xmlns:models="clr-namespace:Moodex.Models"
        xmlns:vm="clr-namespace:Moodex.ViewModels"
        Title="Moodex 1.0"
        Height="600" Width="800"
        WindowState="Maximized"
        Icon="/Moodex;component/Assets/icon/icon.ico"
        Background="#1e1e1e">

    <Window.Resources>
        <conv:FreezingBitmapConverter x:Key="FreezingBitmap"/>
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
        <!-- Dark ComboBox style to match search bar -->
        <Style x:Key="TopBarComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="Bd" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                            <!-- Original working layout: arrow button on right; content presenter on left -->
                            <DockPanel Margin="6,0">
                                <ToggleButton DockPanel.Dock="Right"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Focusable="False" Background="Transparent" BorderThickness="0" Padding="4">
                                    <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#BBBBBB"/>
                                </ToggleButton>
                                <ContentPresenter Margin="2,0" VerticalAlignment="Center" HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  IsHitTestVisible="False"/>
                            </DockPanel>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border Background="#2a2a2a" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2f2f2f"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#666666"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Dark ComboBoxItem style: light grey text; dark hover/selection -->
        <Style x:Key="TopBarComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="6,4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3a3a3a"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Game tile template applied implicitly to GameInfo items -->
        <DataTemplate DataType="{x:Type models:GameInfo}">
            <StackPanel Width="150" Margin="10" Cursor="Hand" RenderTransformOrigin="0.5,0.5">
                <StackPanel.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </StackPanel.RenderTransform>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.08" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.08" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <StackPanel.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.RunCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding}"/>
                </StackPanel.InputBindings>

                <Grid>
                    <Border x:Name="ImageBorder" Background="Black">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Width" Value="150"/>
                                <Setter Property="Height" Value="200"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ConsoleId}" Value="playstation1">
                                        <Setter Property="Width" Value="150"/>
                                        <Setter Property="Height" Value="150"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ConsoleId}" Value="playstation3">
                                        <Setter Property="Height" Value="180"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ConsoleId}" Value="gameboyadvance">
                                        <Setter Property="Width" Value="150"/>
                                        <Setter Property="Height" Value="150"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Image x:Name="TileImage" Source="{Binding GameCoverUri, Converter={StaticResource FreezingBitmap}}" Stretch="Fill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Opacity" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsInArchive}" Value="True">
                                                <Setter Property="Opacity" Value="0.25"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                                <Setter Property="Opacity" Value="0.4"/>
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard x:Name="PulseOpacity">
                                                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.4" Duration="0:0:1.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="PulseOpacity"/>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.6"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <Border Background="#AA000000" Visibility="Collapsed">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="Processing..." Foreground="White" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding ProcessingPercent, StringFormat={}{0:F0}%}" Foreground="White" FontSize="12" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>

                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" Margin="5,5,5,0" TextAlignment="Center" TextWrapping="Wrap"/>

                <StackPanel.ContextMenu>
                    <ContextMenu Background="#2a2a2a" Foreground="White" Opened="GameContextMenu_Opened">
                        <MenuItem Header="Run" Command="{Binding DataContext.RunCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PlacementTarget.DataContext.IsInArchive, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                        <MenuItem Header="Edit" Command="{Binding DataContext.EditGameCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PlacementTarget.DataContext.IsInArchive, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                        <MenuItem Header="Delete" Command="{Binding DataContext.DeleteGameCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="Send to Archive" Visibility="{Binding PlacementTarget.DataContext.IsInArchive, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" Command="{Binding DataContext.ArchiveGameCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="Open File Location" Command="{Binding DataContext.OpenGameFolderCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="Make Active" Visibility="{Binding PlacementTarget.DataContext.IsInArchive, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource BoolToVis}}" Command="{Binding DataContext.ActivateGameCommand, Source={x:Reference RootWindow}}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        <MenuItem Header="Scripts">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource ContextMenuItemStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding DataContext.AutoHotKeyInstalled, Source={x:Reference RootWindow}}" Value="True"/>
                                                <Condition Binding="{Binding PlacementTarget.DataContext.IsInArchive, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                        <MenuItem Header="Controller">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource ContextMenuItemStyle}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PlacementTarget.DataContext.IsInArchive, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                    </ContextMenu>
                </StackPanel.ContextMenu>
            </StackPanel>
        </DataTemplate>

        <!-- Bridge: when the item is GenreGameItem, render the inner Game using the implicit GameInfo template -->
        <DataTemplate DataType="{x:Type vm:GenreGameItem}">
            <ContentPresenter Content="{Binding Game}"/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <!-- ─── Toolbar / Menu ─────────────────────────────────────────── -->
        <Menu DockPanel.Dock="Top" Background="#292929" Foreground="White">
            <MenuItem Header="Games">
                <MenuItem Header="Add" Command="{Binding AddGameCommand}" />
            </MenuItem>
            <MenuItem Header="Emulators">
                <MenuItem Header="Add" Command="{Binding AddEmulatorCommand}" />
                <MenuItem Header="Manage" Command="{Binding ShowManageEmulatorsCommand}" />
            </MenuItem>
            <MenuItem Header="Settings">
                <MenuItem Header="General" Command="{Binding ShowSettingsCommand}" CommandParameter="General"/>
                <MenuItem Header="Input" Command="{Binding ShowSettingsCommand}" CommandParameter="Input"/>
                <MenuItem Header="Interface" Command="{Binding ShowSettingsCommand}" CommandParameter="Interface"/>
                <MenuItem Header="Library" Command="{Binding ShowSettingsCommand}" CommandParameter="Library"/>
                <MenuItem Header="Storage" Command="{Binding ShowSettingsCommand}" CommandParameter="Storage"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Command="{Binding ShowAboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- ─── Top Bar: Search, Group By, Show Archived ────────────────── -->
        <Border DockPanel.Dock="Top" Background="#161616" Padding="80,10,80,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="0"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search bar (full-width) -->
                <Grid Grid.Column="0" Height="30">
                    <Border Background="#2a2a2a" CornerRadius="4" BorderBrush="#555" BorderThickness="1"/>
                    <Grid Margin="6,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔎" Margin="0,0,6,0" VerticalAlignment="Center" Foreground="#bbb"/>
                        <TextBox x:Name="SearchBox" Grid.Column="1"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Foreground="White" Background="Transparent" BorderThickness="0"
                                 CaretBrush="#CCCCCC"
                                 Padding="0" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="2" Content="✕" Width="22" Height="22" Margin="6,0,0,0"
                                Command="{Binding ClearSearchCommand}"
                                Visibility="{Binding Text, ElementName=SearchBox, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                    </Grid>
                </Grid>

                <!-- Group By -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="25,0,25,0">
                    <TextBlock Text="Group By:" Foreground="#838383" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Width="140" Height="28" VerticalContentAlignment="Center" Style="{StaticResource TopBarComboStyle}"
                              SelectedValue="{Binding GroupBy, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="Content"
                              ItemContainerStyle="{StaticResource TopBarComboItemStyle}">
                        <ComboBoxItem Content="Console"/>
                        <ComboBoxItem Content="Genre"/>
                        <ComboBoxItem Content="Year"/>
                    </ComboBox>
                </StackPanel>

                <!-- Show Archived toggle and banner -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Show Archived:" Foreground="#838383" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ToggleButton IsChecked="{Binding ShowArchived, Mode=TwoWay}" Width="28" Height="28">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border Background="#2a2a2a" BorderBrush="#555" BorderThickness="1" CornerRadius="3">
                                    <TextBlock x:Name="Chk" Text="✔" Foreground="LimeGreen" FontSize="14" Margin="0,0,0,4"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Chk" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.6"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                    <TextBlock Text="{Binding ProcessingBannerText}" Foreground="#A0FFA500" VerticalAlignment="Center" Margin="12,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ProcessingBannerText}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ─── Game List ───────────────────────────────────────────────── -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DisplayView}">
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="20,20,10,10"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
